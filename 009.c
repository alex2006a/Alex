#include <stdio.h>

int main()
{
    int n, i, sum = 0,x;
    float avg=0;
printf("%d",n);
    scanf("%d",&n);
    for(i=0;i<n;i++){
     
        sum+=n;
        avg=(sum/x);

    }

    printf("%f",avg)
    return 0;
}